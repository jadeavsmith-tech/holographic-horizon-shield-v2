name: CI
on: [push]
jobs:
  run-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Run Tests
        run: pip install pytest && if [ -f requirements.txt ]; then pip install -r requirements.txt; fi && pytest test_shield.py
