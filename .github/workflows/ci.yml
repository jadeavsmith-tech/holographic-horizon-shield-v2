{"name": "CI", "on": ["push"], "jobs": {"run-tests": {"runs-on": "ubuntu-latest", "steps": [{"name": "Checkout Code", "uses": "actions/checkout@v4"}, {"name": "Run Diagnostics", "run": "pip install pytest pytest-asyncio && pip install --no-cache-dir torch transformers --index-url https://pytorch.org && if [ -f requirements.txt ]; then pip install -r requirements.txt; fi && pytest -v > test_results.log 2>&1 || cat test_results.log"}]}}}
