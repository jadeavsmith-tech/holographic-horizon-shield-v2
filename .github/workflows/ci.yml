{"name": "CI", "on": ["push"], "jobs": {"run-tests": {"runs-on": "ubuntu-latest", "steps": [{"name": "Checkout Code", "uses": "actions/checkout@v4"}, {"name": "Run Tests", "run": "pip install pytest && if [ -f requirements.txt ]; then pip install -r requirements.txt; fi && PYTHONPATH=. pytest test_shield.py"}]}}}
